'use strict';

class Account {
		_scoutId: string;
		_scoutName: string;
		_startBalance: number;
		_endBalance: number;
		constructor(public scoutId: string, public scoutName: string, public startBalance: number, public endBalance: number){
			this._scoutId = scoutId;
			this._scoutName = scoutName;
			this._startBalance = startBalance;
			this._endBalance = endBalance;
		}
}

class Account1617 extends Account {
	

	
}


