'use strict';

class Administrator {
    constructor(
        adminUserId,
        adminName,
        adminPassword
    ) {
        this.adminUserId = adminUserId,
        this.adminName = adminPassword
    }
}

module.exports = Administrator;